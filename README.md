# CheckersOnline
Checkers Online is a multiplayer browser Game for playing Checkers.
## Get Started
### Install & Build
1. Client
    - `cd client`
    - `npm install`
    - `npm run build`
2. Server
   - `cd server`
   - `npm install`
   - `npm run build`
### Start
- `cd server`
- `npm run start`